# SPEC.md — コード進行テキスト → ギターアルペジオ再生

## 0. 目的とスコープ

### 目的

rechord 互換のコード進行テキストを入力として受け取り、**実際のギターの押さえ方に準拠した**運指を自動決定し、アルペジオとして再生する。

### 非目標

- rechord の音楽理論的厳密さの再現（異名同音の区別など）は追わない。指板上で同じ位置なら同じものとして扱う。
- 採譜・音声解析は範囲外。入力はテキストのみ。
- 楽譜（五線譜）出力は範囲外。タブ譜のみ。

### 設計上の基本方針

1. **ギター物理が最上位の制約**。理論的に正しくても押さえられないものは不正解。
2. **中間表現をテキストで持つ**。各層の出力を人が読み書きでき、部分的に上書きできる。
3. **時間は有理数**。固定グリッドを前提にしない。

---

## 1. 全体パイプライン

```
L0    コード進行テキスト（rechord 互換）
 │    正規化 → トークナイズ → 時間割り当て
L0.5  和音の音度スロット表現 + 有理数タイムライン
 │    ドロップ適用 → 候補列挙 → ビタビ DP
L1    ボイシング列（フレットベクトル）
 │    パターントラック合成 → 量子化 → 弦遷移イベント生成
L2    タブ譜（時間 × 弦 × フレット + アーティキュレーション）
 │    ボイス割り当て
音    Karplus-Strong × 6
```

各層は独立にテスト可能でなければならない。L0.5 と L1 は CLI 単体で動くこと。

---

## 2. L0 — 入力記法と時間モデル

### 2.1 正規化

処理順序は厳守する。**記号正規化を root 切り出しより先にやると壊れる**（`C#5` = 「C# の omit3」であり aug ではない）。

1. NFKC 正規化（全角 → 半角）
2. 記号の同一視
   - `＃` `♯` `#` → `#`
   - `♭` `ｂ` `b` → `b`
   - `△` `Δ` → `M`
   - `Φ` `φ` `ø` → `dim` 系（半減の扱いは 3.3 参照）
   - `|` `｜` `l` `ｌ` → 小節線
3. `#` から行末まではコメントとして破棄
4. 小節線で分割
5. 各小節内をトークナイズ（root 最長一致 → 修飾子列 → `/bass`）

### 2.2 時間モデル（最重要）

**小節の分割数は宣言されず、トークン数から導出される。**

| 記号 | 意味 |
|---|---|
| （コード） | 新しい和音。1 スロット消費 |
| `=` | タイ。1 スロット消費し、直前の和音を延長 |
| `_` | 休符。1 スロット消費 |
| `%` | 前の小節と同一内容 |
| `N.C.` | 無和音。1 スロット消費 |

小節内トークン数を N とすると、各スロットの長さは `小節長 / N`。

```
A        → N=1  → A: 1/1
A=       → N=2  → A: 2/2 = 1/1
A===     → N=4  → A: 4/4 = 1/1
AG       → N=2  → A: 1/2, G: 1/2
A==G     → N=4  → A: 3/4, G: 1/4
AG==     → N=4  → A: 1/4, G: 3/4
```

**帰結：**

- N は 3 や 5 にもなる。連符が自然に書ける。
- 長さは既約分数で保持する。**浮動小数は 1/3 で破綻するため禁止**。
- 固定グリッド（`div=1/16` 等）を内部表現の前提にしてはならない。

### 2.3 パースエラー

- 小節先頭の `=`（直前の和音が存在しない）
- 小節先頭以外の `%`
- root として解釈できないトークン

### 2.4 小節をまたぐタイ

`|A|=G|` は前小節末の和音を次小節先頭スロットへ延長する。運指の継承（4.5）を伴う。

### 2.5 テストケース（必須）

| 入力 | 期待 |
|---|---|
| `A` / `A=` / `A===` | 3 つすべて同一の結果（A が小節全体） |
| `AG` / `A==G` / `AG==` | 3 つすべて異なる結果 |
| `A=G` | A: 2/3, G: 1/3（有理数で厳密に一致） |
| `_A` | 休符 1/2 → A 1/2 |
| `A=\|=G` | A が小節をまたいで 1 + 1/2 小節 |
| `=A` | パースエラー |

有理数の等価判定まで含めてこの表が緑になることが、L0 の完了条件。

---

## 3. L0.5 — 和音の内部表現

### 3.1 構造体

```
Chord {
  root_pc:  0..11          # pitch class。綴りは捨てる
  root_spelling: string    # 表示用にのみ保持
  bass_pc:  0..11 | null   # /X 指定
  slots: {
    1:  int | null,        # 値 = ルートからの半音変化量
    3:  int | null,
    5:  int | null,
    7:  int | null,
    9:  int | null,
    11: int | null,
    13: int | null,
  }
  drop_octave: Set<degree> # add2/4/6 系。12 下げる
}
```

初期値は `slots = {1:0, 3:0, 5:0}`、他は `null`。

`slots` の値は**基準音からの相対変化量**であり、絶対音度ではない。これにより `dim` のような相対演算子が素直に書ける。

### 3.2 なぜ辞書引きにしないか

コードタイプ名 → 構成音の辞書方式は次の 3 点で破綻する。

- `dim` は「3rd と 5th を**相対的に** 1 下げる」**演算子**である。`sus2dim` は `sus2` が 3rd を動かした後に `dim` がさらに動かす（順序依存）。
- `6` は「7th を 1 下げたもの」であり、6th と 7th は**同一スロットを共有**する。`C67` == `C7` はこの帰結。
- `add9` と `add2` はスロットが同じで**配置オクターブだけが違う**。

したがって**修飾子を左から順に適用する関数列**として実装する。

### 3.3 修飾子テーブル

| トークン | 作用 |
|---|---|
| `m` | `slots[3] -= 1` |
| `aug` `+` `+5` | `slots[5] += 1` |
| `-5` | `slots[5] -= 1` |
| `sus4` | `slots[3] += 1` |
| `sus2` | `slots[3] -= 2` |
| `dim` `o` | `slots[3] -= 1; slots[5] -= 1` |
| `dim7` | `slots[3] -= 1; slots[5] -= 1; slots[7] = -2` |
| `Φ` `φ` `ø` | `slots[3] -= 1; slots[5] -= 1; slots[7] = -1`（半減七） |
| `7` | `slots[7] = -1` |
| `9` | `slots[7] = -1; slots[9] = 0` |
| `11` | `slots[7] = -1; slots[9] = 0; slots[11] = 0` |
| `13` | `slots[7] = -1; slots[9] = 0; slots[11] = 0; slots[13] = 0` |
| `M7` `maj7` | `slots[7] = 0` |
| `M9` `maj9` | `slots[7] = 0; slots[9] = 0` |
| `M11` `maj11` | `slots[7] = 0; slots[9] = 0; slots[11] = 0` |
| `M13` `maj13` | `slots[7] = 0; …; slots[13] = 0` |
| `add9` | `slots[9] = 0` |
| `add11` | `slots[11] = 0` |
| `add13` | `slots[13] = 0` |
| `add2` | `slots[9] = 0; drop_octave += {9}` |
| `add4` | `slots[11] = 0; drop_octave += {11}` |
| `add6` `6` | `slots[13] = 0; drop_octave += {13}` |
| `omit1` `no1` | `slots[1] = null` |
| `omit3` `no3` `5` | `slots[3] = null` |
| `omit5` `no5` | `slots[5] = null` |
| `(±n)` | 対応スロットに変化量を代入（例：`(-9)` → `slots[9] = -1`） |

- `omit` / `no` は対象スロットが `null` の場合 no-op。
- `M` `maj` 単体は無効（7 以上の数値が必須）。`CM` は C メジャートライアドとして扱う例外。
- 括弧は `(no…` のみ片側可。テンション `(-9)` は両側必須。

### 3.4 テストケース（必須）

| 入力 | 期待 pitch class（root = C = 0） |
|---|---|
| `Csus2dim` | {0, 2, 6}（Ebbb ≡ D、Gb） |
| `C67` | `C7` と一致 |
| `Cdim7` | {0, 3, 6, 9} |
| `Cdim` + `7`（`Cdim7` ではない書き方） | `Cdim7` と**不一致**であること |
| `C#5` | C# の omit3 = {1, 8} |
| `C(#5)` | C aug = {0, 4, 8} |
| `Cb5` | Cb の omit3 = {11, 6} |
| `Comit1` | {4, 7} |

pitch class 集合での比較とする（綴りは検証しない）。

---

## 4. L1 — ボイシングソルバ

### 4.1 音の取捨（ドロップ優先順位）

6 弦・4 指では 13th 系（7 音）は物理的に不可能。**省略は例外処理ではなく通常動作**として設計する。

**保持（原則落とさない）**
- root
- 3rd（または sus で置換された音）
- 7th
- 特性音：変化量 ≠ 0 のスロット全て、`add` 系スロット、6th

**落とす順序**

```
5th → 11th → 9th → root（bass 弦で確保済みの場合のみ） → 13th
```

`null` スロットは候補音名から**除外**する（`omit5` の 5th は鳴らしてはならない）。

### 4.2 候補列挙

- 6 弦 × 0〜15 フレット
- 各弦の選択肢＝許可音名に一致するフレット群 + ミュート（1 弦あたり 5〜6 通り）
- 全組合せは約 1.5 万。総当たりで十分高速

**フィルタ**

| 条件 | 内容 |
|---|---|
| フレット幅 | 押弦フレットの max − min ≤ 4（バレーは例外扱い） |
| 内声ミュート | 鳴弦に挟まれたミュートは最大 1 本。指の腹ミュートが物理的に成立する位置のみ |
| 必要指数 | バレー統合後 ≤ 4 |
| 音の充足 | 4.1 の「保持」対象が全て存在 |
| bass 制約 | `/X` があれば最低鳴弦の pitch class == X |
| 鳴弦数 | ≥ 4（アルペジオはボイス数が必要）。パワーコード系は例外で ≥ 2 |

### 4.3 単体コスト

| 項目 | 方向 |
|---|---|
| 使用指数 | 少ないほど良 |
| フレット幅 | 狭いほど良 |
| バレー成立 | ボーナス |
| 開放弦の数 | ボーナス |
| ポジションの高さ | 低いほど良 |
| root がベース位置 | ボーナス |
| 上声部の半音隣接 | ペナルティ（ただし 4.4 の例外あり） |
| ギター慣習形 | ベースは 6/5/4 弦、内声は drop2/drop3 相当、トップは 1/2/3 弦 |

### 4.4 `drop_octave` によるコスト分岐

`add9` と `add2` は同じスロットだが**配置が違い、響きが全く変わる**。

- `add9`（drop なし）→ 9th を高音弦に置く配置を優先
- `add2`（drop あり）→ 内声クラスタを許容し、**上声部の半音隣接ペナルティを緩める**

`add4/add11`、`add6/add13` も同様。

### 4.5 遷移コスト

| 項目 | 方向 |
|---|---|
| 左手重心フレットの移動距離 | 小さいほど良 |
| 同一弦・同一フレット（非開放）の保持数 | ボーナス |
| 開放弦の継続使用 | ボーナス |
| 鳴弦セットの差分 | ペナルティ（パターン安定化 + ダンプ回数削減の両方に効く） |
| トップノートの跳躍量 | 小さいほど良 |

### 4.6 探索

- 各コードで単体コスト上位 K（20〜40）に枝刈り
- ビタビ DP、O(N · K²)。N は数十程度なので体感ゼロ
- **k-best 経路で「別解」を提示できる**ようにする（UI 機能として有用）

**ピン留め（再探索の禁止）**

- `=`（タイ）→ 前ノードの運指を継承。DP ノードを固定
- `%` → 前小節の運指列をそのまま継承

### 4.7 L1 テキスト形式

有理数の時間位置を持つため、列アラインメント方式は採らず**位置明示**とする。

```
@meter 4/4
@tuning EADGBE
@capo 0

# bar:offset   chord      voicing        note
5:0/1          Cm         x-3-5-5-4-3
5:1/2          G7omit5    3-x-3-4-x-x
6:0/1          Eb6        x-6-5-5-4-x    # 5th 省略
6:1/2          ~                         # タイ：運指継承
7:0/3          Fm/Ab      4-x-3-1-1-x
7:1/3          _                         # 休符：全鳴弦ダンプ
7:2/3          Dm7(b5)    x-5-6-5-6-x
8:0/1          Gsus4/D    x-5-5-5-3-3
```

- `bar:offset` の offset は既約分数
- フレットは常にハイフン区切り（`x35543` 形式は 10 フレット以上で壊れるため禁止）
- `x` = ミュート、`0` = 開放
- `~` = タイ、`_` = 休符
- 運指番号は保存せず毎回計算（バレー判定込み）。人が明示する場合のみ `:f=x134 21` を後置

上の voicing 例は参考値であり、ソルバの出力で置き換えられる。

---

## 5. L2 — タブ譜

### 5.1 2 トラック独立

**パターンをコードに従属させてはならない。** `AG==` のように和音が 1/4 しか続かない場合、和音ごとにパターンを再起動すると細切れになり演奏として成立しない。

- **ボイシングトラック**：時間 → 運指の階段関数
- **パターントラック**：小節・フレーズをまたぐ連続タイムライン

L2 生成は**パターン側のグリッドで走査**し、各撥弦時刻にボイシングトラックを引いて解決する。パターンはコード変更で途切れない。

### 5.2 パターンは役割参照で書く

鳴弦セットは運指によって変わる（`x-3-5-5-4-3` は 5 本、`x-x-6-5-4-3` は 4 本）。絶対弦番号で書くと破綻するため、**役割**で定義する。

| 役割 | 意味 |
|---|---|
| `B` | 最低鳴弦 |
| `B*` | 交互ベース（`Ab/F` なら F ↔ Ab を往復） |
| `m1` `m2` | 内声（低い側から） |
| `T` | 最高鳴弦 |

```
pattern travis div=1/8 {
  B  m2  T  m1  |  B*  m2  T  m1
}
```

### 5.3 グリッド衝突の量子化

N=3 の小節に 1/8 パターンを乗せると、コード切替が撥弦の中間に落ちる。左手は撥弦の途中で移れない。

- **最近傍の撥弦オンセットに量子化**する
- 等距離の場合は早い側に寄せる（新コードのベース音がアタックを取るほうが自然）
- ズレ量を警告として出力する
- LCM を取って細分化する（LCM(3,8)=24 等）のは音楽的に無意味なので**行わない**

### 5.4 弦遷移イベント（鍵盤モデルとの決定的な差）

コード変更時、各弦を 3 分類してイベントを立てる。

| 弦の状態変化 | 必要なイベント |
|---|---|
| 押弦フレットが変わる | 自然リリース（新フレットを押さえた瞬間に前音が止む） |
| 鳴弦 → ミュート | **明示ダンプが必要** |
| 開放 → 開放（継続） | 何もしない。鳴らし続ける |

「鳴弦 → ミュート」を落とすと前の和音が残って濁る。

### 5.5 休符の実体

**指を離すだけでは開放弦が鳴る。** `x-3-5-5-4-3` から離せば開放 A D G B E が響いてしまう。

休符では**全鳴弦に明示ダンプイベントを発行**する。手段（右手ブラッシング / 左手の腹）はアーティキュレーションフラグとして持つ。

### 5.6 サステイン解決

- 各弦は独立した単音ボイス
- 同じ弦の次イベントまで音は持続する（暗黙のダンパー）
- 押弦フレットが変わる弦は強制リリース
- 開放弦は小節をまたいで鳴り続ける

この規則を守るだけでリアルさが大きく変わる。全て同じ長さで切ると打ち込み臭くなる。

### 5.7 L2 テキスト形式

ASCII タブを主形式とする。素の ASCII タブは列アラインメントが壊れやすいため、**セクション単位でグリッド分割を明示し、列幅を固定**する。

```
@section bar=5 div=1/8

1|--------------8-  ----------------
2|----------4-----  --------3-------
3|------5---------  ----4-----------
4|--5-------------  --3-------------
5|----------------  ----------------
6|8---------------  ----------------
```

- 弦番号は 1 = 最高音弦（慣習に従う）
- 列インデックス = グリッド位置（パーサは厳格に列で読む）
- アーティキュレーション：`h` ハンマリング、`p` プリング、`/` `\` スライド、`~` ビブラート、`X` ダンプ / ゴーストノート、`( )` レットリング明示

不規則分割（N=3 等）を含む小節は `div` を分数で宣言する。

厳密なラウンドトリップが必要な場合のフォールバックとしてイベントリスト形式も持つ。

```
bar=7 s6 t=0/3 fret=4 vel=90
bar=7 s4 t=1/6 fret=3
bar=7 all t=1/3 dump
```

---

## 6. 音源

### 方針

Karplus-Strong を AudioWorklet で 6 本並列に走らせる。

**理由**

- 弦別・フレット別サンプルを揃えると数百 MB 級になる
- ギターのアルペジオは減衰系の単純な撥弦音であり、物理モデリングが最も割に合う音色領域
- 弦ごとの減衰時間、ピッキング位置、ピッキング強度を連続パラメータとして持てる
- データ量ゼロ

サンプルの生々しさは出ないが、コード確認用途としては十分。将来的にサンプラを差し替え可能なインタフェースにしておく。

### パラメータ

| 項目 | 内容 |
|---|---|
| 弦 | 6 本を独立ボイスとして固定割り当て |
| 減衰 | 低音弦ほど長い |
| ピッキング位置 | 明るさに対応 |
| ダンプ | 減衰時間を急峻に切り替える |

---

## 7. 実装順序と完了条件

| 順 | 対象 | 完了条件 |
|---|---|---|
| 1 | L0 パーサ + 時間モデル | 2.5 のテスト表が全て緑（有理数の厳密一致まで） |
| 2 | L0.5 修飾子パイプライン | 3.4 のテスト表が全て緑 |
| 3 | L1 ソルバ（CLI） | コードテキスト → L1 テキストを出力。音を出さずに目で「実際に押さえる形か」を判定できる |
| 4 | L1 → L2 展開器 + タブ表示 | パターン 1〜2 種で通る |
| 5 | 音（Karplus-Strong） | 耳で「ギターに聞こえるか」を判定 |

**1 と 2 が緑になるまで 3 に進まない。** ここが最も安く固められる部分であり、後段のバグの大半はここに起因する。

3 のコスト関数の重み調整が最も工数を食う。k-best 出力を早めに用意して、複数解を見比べながら重みを詰める。

---

## 8. 未確定事項

| 項目 | 状態 |
|---|---|
| rechord の `<` `>`（途中再生マーカー） | 再生開始位置。L2 のグリッド位置へ伝播させる方針だが優先度低 |
| `Φ` `φ` `ø` の解釈 | 半減七（m7b5）として実装。rechord 実機との差異は許容する |
| バレー成立条件のモデル化 | 内声ミュートと合わせて最も泥臭い部分。ヒューリスティクスは耳で調整する前提 |
| パターンライブラリ | Travis、3 フィンガー、順次上昇下降など。MVP は 1〜2 種 |
| カポ | `@capo` を持つが、ソルバ側は capo を 0 フレット読み替えで処理 |
